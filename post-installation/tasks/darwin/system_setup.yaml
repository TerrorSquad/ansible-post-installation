---
# System preparation for macOS
- name: Prepare download directory
  ansible.builtin.file:
    path: "{{ download_dir }}"
    state: directory
    mode: '0755'

- name: Install Xcode Command Line Tools and basic packages
  ansible.builtin.include_tasks: darwin/basic_packages.yaml

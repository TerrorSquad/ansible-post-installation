---
# Development foundation: version control and editors
- name: Install and configure Git
  ansible.builtin.include_tasks: shared/git.yaml

- name: Configure Neovim
  ansible.builtin.include_tasks: "{{ ansible_os_family | lower }}/nvim.yaml"

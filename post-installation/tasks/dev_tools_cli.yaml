---
- name: Install apt apps - curl, VIm, Terminator, RedShift, htop
  ansible.builtin.apt:
    name:
      - bat
      - curl
      - zip
      - unzip
      - htop
      - libutempter0
      - make
      - shellcheck
      - vim
      - python3-pip
      - jq

# - name: Install SDKMan
#   ansible.builtin.shell: |
#     curl -s "https://get.sdkman.io" | bash
#   become_user: "{{ username }}"

- name: Copy Zscaler root cert to $HOME
  ansible.builtin.copy:
    src: ./defaults/.ZscalerRootCA.crt
    dest: "{{ user_home }}"
    mode: "0644"
  become_user: "{{ username }}"

- name: Install Volta
  ansible.builtin.shell: |
    curl https://get.volta.sh | bash
  become_user: "{{ username }}"

- name: Install latest NodeJS using Volta
  ansible.builtin.shell: >
    zsh -i -c "volta install node"
  become_user: "{{ username }}"

- name: Install latest PNPM using Volta
  ansible.builtin.shell: >
    zsh -i -c "volta install pnpm"
  become_user: "{{ username }}"

- name: Install homebrew packages
  ansible.builtin.shell: |
    HOMEBREW_NO_AUTO_UPDATE=1 HOMEBREW_NO_ENV_HINTS=1 /home/linuxbrew/.linuxbrew/bin/brew install \
      bottom \
      broot \
      btop \
      curlie \
      duf \
      dust \
      eza \
      fd \
      gh \
      git-quick-stats \
      gping \
      gping \
      httpstat \
      hyperfine \
      lnav \
      oha \
      procs \
      ripgrep \
      sd \
      tealdeer \
      tokei \
      xh \
      yazi

  become_user: "{{ username }}"

- name: Install Rust tools
  ansible.builtin.shell: |
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
  become_user: "{{ username }}"

- name: Generate Rust completion scripts
  ansible.builtin.shell: |
    /home/{{ username }}/.cargo/bin/rustup completions zsh > /home/{{ username }}/.zsh/completions/_rustup && \
    /home/{{ username }}/.cargo/bin/rustup completions zsh cargo > /home/{{ username }}/.zsh/completions/_cargo
  become_user: "{{ username }}"

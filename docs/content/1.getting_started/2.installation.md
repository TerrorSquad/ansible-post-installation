---
title: Installation Guide
description: How to install Griffin and its dependencies
navigation:
  icon: i-heroicons-arrow-down-tray
---

## Installation Steps

::steps
  ### Check Prerequisites
  Ensure Python and Ansible are installed.
  
  ::code-group
    ```bash [Linux]
    ansible --version
    unzip --version
    ```
    ```bash [macOS]
    ansible --version
    ```
  ::

  ### Install Dependencies (If Needed)
  ::code-group
    ```bash [Debian/Ubuntu]
    sudo apt update && sudo apt install ansible unzip
    ```
    ```bash [macOS Homebrew]
    brew install ansible
    ```
    ```bash [macOS Pip]
    pip3 install ansible
    ```
  ::

  ### Corporate Proxy Setup (Optional)
  If you are behind a corporate proxy (e.g., Zscaler), install the root certificate:
  
  ```bash
  curl -k -s https://raw.githubusercontent.com/TerrorSquad/ansible-post-installation/master/add_zscaler_root_cert.sh | bash
  ```
::

## Troubleshooting

If you encounter any issues during the installation, refer to the [FAQ](/support/faq) for assistance.

## Next Steps

Explore the features and customizations **Griffin** offers.
Learn how to contribute to the project.
